import { Heading } from "@/components/Heading/Heading";
import { FC } from "react";

const About: FC = ({}): JSX.Element => {
  return <Heading tag={"h2"} text={"About"} />;
};

export default About;
