import { Heading } from "@/components/Heading/Heading";
import { FC } from "react";

const Profile: FC = (): JSX.Element => {
  return <Heading tag={"h2"} text={"Profile"} />;
};

export default Profile;
