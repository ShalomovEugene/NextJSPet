import { Heading } from "@/components/Heading/Heading";
import { FC } from "react";

const Features: FC = ({}): JSX.Element => {
  return <Heading tag={"h2"} text={"Features"} />;
};

export default Features;
